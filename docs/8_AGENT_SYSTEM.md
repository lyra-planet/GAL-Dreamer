# GAL-Dreamer 8-Agent系统架构文档

## 📖 系统概述

GAL-Dreamer采用**8个专业Agent协作**的架构,将用户的一段故事创意自动转化为完整的GALGAME剧本。

### 核心理念

> **用户只给"故事设想文本",剩下所有Galgame的"世界—结构—剧情—角色—文本"全部由Agent体系自动生成**

---

## 🎯 设计原则

### ✅ 必须遵守

1. **职责单一**: 每个Agent只做一件事,做好一件事
2. **JSON中间格式**: 前7个Agent都输出结构化JSON,便于验证和传递
3. **只有Narrator输出自然语言**: 最后一个Agent才生成最终文本
4. **世界观是"法律"**: 所有剧情不能违反世界观规则
5. **结构优先**: 先有完整结构,再填充细节

### ❌ 严禁操作

- 不要让一个Agent从头写到尾
- 不要让Narrator参与结构设计
- 不要跳过一致性检查
- 不要让Agent自由发挥过度

---

## 🤖 8个Agent详解

### 1️⃣ Story Intake Agent (故事理解Agent)

**唯一直接读取用户输入的Agent**

#### 职责
- 理解用户的"故事创意文本"
- 抽象出题材、主题、风格、限制
- 防止后续Agent过度发挥

#### 输入
```python
{
    "user_idea": "一段自然语言的剧情设想/点子/灵感"
}
```

#### 输出
```json
{
    "genre": "恋爱+悬疑",
    "themes": ["信任", "选择的代价"],
    "tone": "偏现实,略带压抑",
    "must_have": ["多女主", "多结局"],
    "forbidden": ["超自然元素"]
}
```

#### 关键点
- genre必须是单个明确题材
- themes列出2-5个核心主题
- forbidden是"硬约束",后续Agent不得违反

---

### 2️⃣ Worldbuilding Agent (世界观构建Agent)

**整个系统的"物理定律"**

#### 职责
- 构建世界背景、时代、规则
- 明确"什么能发生/什么不能发生"
- 为一致性提供硬约束

#### 输入
```python
{
    "story_constraints": "Story Intake的输出",
    "genre": "题材信息",
    "themes": ["主题列表"]
}
```

#### 输出
```json
{
    "era": "现代都市",
    "location": "东京",
    "type": "现实向",
    "rules": [
        {"rule_id": "rule1", "description": "没有超自然元素", "is_breakable": false},
        {"rule_id": "rule2", "description": "社会规则与现实一致", "is_breakable": false}
    ],
    "core_conflict_source": "信息不对称导致的不信任",
    "description": "现代都市背景下的校园恋爱故事...",
    "special_elements": []
}
```

#### 关键点
- rules是"硬约束",剧情不得违反
- core_conflict_source必须根植于世界设定
- 特殊元素(special_elements)如果没有则为空列表

---

### 3️⃣ Cast Design Agent (角色群像设计Agent)

**生成所有人物**

#### 职责
- 设计主角(玩家)
- 设计可攻略角色(女主/男主)
- 设计关键配角
- 明确角色秘密、动机、冲突点

#### 输入
```python
{
    "world_setting": "Worldbuilding的输出",
    "themes": ["主题列表"],
    "required_routes": 3  # 需要几条攻略线
}
```

#### 输出
```json
{
    "protagonist": {
        "character_id": "protag_001",
        "name": "佐藤健一",
        "age": 17,
        "personality": ["内向", "善良", "优柔寡断"],
        "background": "普通高中生...",
        "core_flaw": "逃避冲突",
        "motivation": "寻求真相"
    },
    "heroines": [
        {
            "character_id": "heroine_001",
            "name": "星野爱",
            "personality": ["开朗", "神秘"],
            "secret": "实际上是转校生前的受害者",
            "key_conflict_points": ["身份隐瞒", "过去创伤"]
        }
    ],
    "side_characters": [...],
    "character_relationships": {
        "protag_001-heroine_001": "暗恋关系",
        "heroine_001-heroine_002": "情敌关系"
    }
}
```

#### 关键点
- 主角必须有core_flaw(核心缺陷)
- 每个可攻略角色必须有secret(秘密)
- key_conflict_points至少2-4个
- character_relationships描述所有角色间关系

---

### 4️⃣ Macro Plot Agent (大剧情结构Agent)

**决定"整个故事怎么走"**

#### 职责
- 生成完整故事骨架
- 明确起承转合
- 标记关键转折点
- 不涉及具体台词

#### 输入
```python
{
    "world_setting": "世界观",
    "cast_summary": "角色群像摘要",
    "themes": ["主题列表"]
}
```

#### 输出
```json
{
    "acts": {
        "act1": "日常展示,伏笔埋设,所有角色登场",
        "act2": "矛盾升级,关系发展,初步冲突显现",
        "act3": "真相揭露,冲突爆发,情感高潮",
        "act4": "分线发展,各自结局,主题升华"
    },
    "major_twists": [
        "第3章揭露:女主角A隐瞒了关键真相",
        "第5章转折:主角必须在两人之间做出选择"
    ],
    "story_arc": "一个关于信任与选择的故事",
    "climax_point": "毕业典礼当天的对峙与抉择"
}
```

#### 关键点
- 四幕结构(act1-4)清晰
- major_twists列出3-5个主要转折
- climax_point情感饱满
- 为多线路预留分歧空间

---

### 5️⃣ Route Design Agent (多线路设计Agent)

**Galgame的核心**

#### 职责
- 设计多条可攻略线路
- 决定每条线的核心冲突
- 标记分歧点
- 明确每条线的结局类型

#### 输入
```python
{
    "macro_plot": "Macro Plot的输出",
    "heroine_list": ["可攻略角色列表"]
}
```

#### 输出
```json
{
    "routes": [
        {
            "route_id": "route_A",
            "route_name": "星的轨迹",
            "heroine_id": "heroine_001",
            "branch_point": "第3章选择:是否追问真相",
            "conflict_focus": "信任危机",
            "ending_types": ["Good End", "Bad End", "True End"],
            "route_summary": "揭开过去的创伤,重建信任"
        }
    ],
    "common_route_length": "50%",
    "branching_strategy": "基于关键选择分歧"
}
```

#### 关键点
- 每条线有独特的conflict_focus
- branch_point明确分歧位置
- ending_types至少2种,建议3-4种
- common_route_length描述共通线占比

---

### 6️⃣ Conflict & Emotion Agent (冲突与情绪引擎Agent)

**防止剧情变成流水账**

#### 职责
- 给每条线路注入冲突
- 设计误解、对立、爆点
- 管理情绪曲线(压抑→爆发→释放)

#### 输入
```python
{
    "route_plots": "各线路剧情",
    "character_states": "角色关系与秘密"
}
```

#### 输出
```json
{
    "conflicts": [
        {
            "node_id": "conflict_001",
            "conflict_type": "误解",
            "emotional_intensity": "high",
            "participants": ["heroine_001", "protagonist"],
            "description": "主角发现女主角隐瞒真相",
            "resolution_method": "真诚对话"
        }
    ],
    "emotional_curves": [
        {
            "route_id": "route_A",
            "curve_points": [
                {"position": "0.2", "intensity": "low"},
                {"position": "0.5", "intensity": "high"},
                {"position": "0.8", "intensity": "extreme"},
                {"position": "1.0", "intensity": "medium"}
            ]
        }
    ]
}
```

#### 关键点
- conflict_type: 误解/对立/秘密/选择/背叛/牺牲
- emotional_intensity: low/medium/high/extreme
- 每条线至少3个冲突节点
- 情绪曲线有起伏,避免平铺直叙

---

### 7️⃣ Consistency Agent (一致性审查Agent)

**剧情"监工"**

#### 职责
- 检查是否吃书(自相矛盾)
- 防止角色OOC(Out Of Character)
- 防止世界规则被破坏
- 提出修改建议

#### 输入
```python
{
    "full_story_structure": "当前全部剧情",
    "world_rules": "世界观规则",
    "character_profiles": "角色人设"
}
```

#### 输出
```json
{
    "valid": true,
    "issues": [
        "第3章时间线与第2章稍有冲突"
    ],
    "lore_violations": [],
    "character_ooc": [
        "第5章女主角A的行为略显OOC"
    ],
    "fix_suggestions": [
        "建议调整第3章时间线",
        "建议增加第5章女主角A的动机铺垫"
    ]
}
```

#### 关键点
- valid: 只有当没有严重问题时才为true
- lore_violations: 世界观违规列表
- character_ooc: 角色OOC列表
- fix_suggestions必须具体可执行

---

### 8️⃣ Narrator Agent (Galgame文本生成Agent)

**最后一步:把"结构"变成"文本"**

#### 职责
- 场景描写
- 心理描写
- 角色对白
- Galgame风格文本输出

#### 输入
```python
{
    "scene_structure": "场景结构",
    "character_actions": "角色行为",
    "tone": "整体基调"
}
```

#### 输出
```text
放学后的教室里,夕阳透过窗户洒进来,将两人的影子拉得很长。

她沉默了一会儿,像是在权衡什么。

"其实,我..."

"?"

"没什么。"

她站起身,背对着你。窗外的城市灯光一盏盏亮起,你却感觉有什么正在悄然崩塌。

"那个..."你试图开口。

"明天见。"

她匆匆离开了教室,留下你一个人站在夕阳中。

"..."
```

#### 关键点
- 只输出纯文本,不包含场景指令
- 符合Galgame文本格式
- 语言流畅自然
- 情感表达细腻

---

## 🔄 完整执行流程

```
用户输入故事创意
    ↓
┌─────────────────────────────────────┐
│  Story Intake Agent                 │
│  提取: genre, themes, tone,         │
│        must_have, forbidden         │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│  Worldbuilding Agent                │
│  构建: era, location, rules,        │
│         core_conflict_source        │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│  Cast Design Agent                  │
│  设计: protagonist, heroines,       │
│         side_characters             │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│  Macro Plot Agent                   │
│  构建: acts, major_twists,          │
│         climax_point                │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│  Route Design Agent                 │
│  设计: routes, branch_points,       │
│         ending_types                │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│  Conflict & Emotion Agent           │
│  注入: conflicts, emotional_curves  │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│  Consistency Agent                  │
│  检查: valid, issues,               │
│         fix_suggestions             │
└─────────────────────────────────────┘
    ↓ (如果valid=false,返回修改)
┌─────────────────────────────────────┐
│  Narrator Agent                     │
│  生成: Galgame风格文本              │
└─────────────────────────────────────┘
    ↓
完整的Galgame剧本
```

---

## 📂 文件结构

```
GAL-Dreamer/
├── models/                    # 数据模型
│   ├── story.py              # 故事相关模型
│   ├── world.py              # 世界观数据模型
│   ├── character.py          # 角色数据模型
│   └── plot.py               # 剧情数据模型
│
├── prompts/                   # Prompt模板
│   ├── story_intake_prompt.py
│   ├── worldbuilding_prompt.py
│   ├── cast_design_prompt.py
│   ├── macro_plot_prompt.py
│   ├── route_design_prompt.py
│   ├── conflict_emotion_prompt.py
│   ├── consistency_prompt.py
│   └── narrator_prompt.py
│
├── agents/                    # Agent实现(待完成)
│   ├── story_intake_agent.py
│   ├── worldbuilding_agent.py
│   ├── cast_design_agent.py
│   ├── macro_plot_agent.py
│   ├── route_design_agent.py
│   ├── conflict_emotion_agent.py
│   ├── consistency_agent.py
│   └── narrator_agent.py
│
└── chains/                    # 链式调用(待完成)
    └── game_generation_chain.py
```

---

## 🎮 使用示例

```python
from gal_dreamer import GALDreamer

# 初始化系统
dreamer = GALDreamer()

# 输入故事创意
user_idea = """
一个现代校园背景的故事。
主角是一个普通高中生,突然班里来了一个转校生。
这个转校生似乎隐瞒了什么秘密。
随着故事发展,主角发现转校生实际上是在躲避什么。
故事要有多条攻略线,每条线有不同的结局。
"""

# 生成完整Galgame剧本
game_script = dreamer.generate(user_idea)

# 输出结果
print(f"游戏标题: {game_script.title}")
print(f"世界观: {game_script.world_setting}")
print(f"角色: {len(game_script.heroines)}个可攻略角色")
print(f"线路: {len(game_script.routes)}条攻略线")
print(f"场景: {len(game_script.scenes)}个场景")

# 保存剧本
game_script.save("output/my_first_galgame.json")
```

---

## ✅ 质量保证

### 一致性检查清单

- [ ] 世界观没有被违反
- [ ] 角色没有OOC
- [ ] 时间线连贯
- [ ] 因果关系合理
- [ ] 转折点有铺垫
- [ ] 结局基于前面发展

### 剧情质量标准

- [ ] 每条线有独特冲突
- [ ] 情绪曲线有起伏
- [ ] 对话符合角色性格
- [ ] 节奏张弛有度
- [ ] 主题得到体现

---

## 🚀 下一步

现在我们有了:
- ✅ 完整的数据模型
- ✅ 8个Agent的Prompt模板
- ✅ 清晰的架构文档

接下来需要:
- [ ] 实现8个Agent类
- [ ] 创建主流程Chain
- [ ] 编写端到端测试
- [ ] 优化Prompt效果

准备好继续实现了吗? 🎯
